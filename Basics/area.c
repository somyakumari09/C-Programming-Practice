#include<stdio.h>

int main(){
    int length;
    int breadth;
    printf("enter length");
    scanf("%d",&length);
    printf("enter breadth");
    scanf("%d",&breadth);
    printf("area of rectangle is %d",length*breadth);
    return 0;
}